# MRP API (NestJS)

Control plane for Circle invites / consent / FCM. **Live location stays on Firebase RTDB.**

```bash
cd api
npm install
npm run start:dev
```

Health: `GET http://localhost:3000/v1/health`

See [MRP/CIRCLE_LIVE.md](../MRP/CIRCLE_LIVE.md).
